name: token-scope-probe
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke
        run: |
          echo "REST_CODE=999"
          echo "GRAPHQL_HAS_REPO=0"
